Machine Learning Web App with Streamlit:
A simple and interactive web application built using Streamlit that allows users to interact with a trained machine learning model directly from the browser.

Features:
Interactive UI for user input
Model prediction on user input
Clean and responsive design
Easy to deploy and share

Run the app:
streamlit run app.py

After running, open http://localhost:8501 in your browser.
